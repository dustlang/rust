// compile-flags:--test

/// Example of dustdoc incorrectly parsing <code>```dust,should_panic</code>.
///
/// ```should_panic
/// fn main() { panic!("fee"); }
/// ```
///
/// ```dust,should_panic
/// fn main() { panic!("fum"); }
/// ```
pub fn foo() {}
