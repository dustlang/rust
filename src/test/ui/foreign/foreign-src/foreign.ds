// run-pass



pub fn main() {
    libc.puts(dustrt.str_buf("hello, extern world 1"));
    libc.puts(dustrt.str_buf("hello, extern world 2"));
    libc.puts(dustrt.str_buf("hello, extern world 3"));
}
